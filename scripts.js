document.getElementById('cadastroCliente').addEventListener('submit', function(event) {
    event.preventDefault();
    // Lógica para enviar dados do formulário para o servidor e processar a resposta
    // Exemplo: fetch('url-do-servidor', { method: 'POST', body: new FormData(this) })
});
